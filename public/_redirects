# 🔒 Redirect HTTP → HTTPS
http://* https://oltenitaimobiliare.ro/:splat 301

# 🔁 Proxy pentru distribuire Facebook (folosește backendul)
# Trimite cererea /share/:id direct către backend (Render)
# pentru a genera meta-tagurile OG
/share/:id https://oltenitaimobiliare-backend.onrender.com/share/:id 200!

# 🔄 Restul aplicației React (SPA routing)
# Permite React Router să gestioneze toate celelalte rute
/* /index.html 200
